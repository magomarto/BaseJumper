// binary : BASE 2 : 0, 1

// fazer função auxiliar que verifica se a string é binária valida
// fazer Função auxiliar que completa com zeros à esquerda para formar grupos de 4 bits

// BIN(AS STRING) TO HEX

// BIN (AS STRING) TO OCTAL

// BIN(AS STRING) TO DECIMAL

