#ifndef BINARY_CONVERTER_H
#define BINARY_CONVERTER_H

char* bin_to_hex(const char* binary); // BIN STRING TO HEX

char* bin_to_octal(const char* binary); // BIN STRING TO OCTAL

int bin_to_decimal(const char* binary); // BIN STRING TO DECIMAL(INT)

#endif