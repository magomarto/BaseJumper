#ifndef OCT_CONVERTER_H
#define OCT_CONVERTER_H

char* oct_to_bin(const char* octal); // OCTAL STRING TO BINARY

int oct_to_decimal(const char* octal);// OCTAL STRING TO DECIMAL (INT)

char* oct_to_hex(const char* octal); // OCTAL STRING TO HEX

#endif